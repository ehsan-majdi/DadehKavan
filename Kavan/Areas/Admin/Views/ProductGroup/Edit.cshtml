@{
    ViewBag.Title = "Edit";
    Layout = "/Views/Shared/_AdminLayout.cshtml";
}

<div class="container">
    <div class="row">
        <div class="col-md-12">
            @using (Html.BeginForm("Update", "ProductGroup", FormMethod.Post))
            {
                <input id="hiddenId" type="hidden" value="@ViewBag.Id" name="Id" />
                <div class="col-md-3">
                    <label>عنوان</label>
                    <input class="form-control" type="text" name="GroupName" value="@ViewBag.productGroup" placeholder="عنوان" />
                </div>
                <button type="submit" class="btn btn-success">ذخیره</button>
            }
        </div>
    </div>
   
</div>

